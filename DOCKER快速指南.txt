╔═══════════════════════════════════════════════════════════╗
║       FFT Solar CRM - Docker 快速指南                     ║
╚═══════════════════════════════════════════════════════════╝

🐳 Docker 端口配置
═══════════════════════════════════════════════════════════
前端 (React + Nginx):   http://localhost:3201
后端 (Node.js API):     http://localhost:3200
数据库 (PostgreSQL):    localhost:3202

🚀 快速部署（3步）
═══════════════════════════════════════════════════════════
1. 确保 Docker Desktop 正在运行
2. 双击 "docker-deploy.bat"
3. 等待 5-10 分钟完成部署

✅ 部署完成后
═══════════════════════════════════════════════════════════
访问: http://localhost:3201
登录: admin / admin123

📦 包含的容器
═══════════════════════════════════════════════════════════
• fft-solar-frontend  - React 前端 (Nginx)
• fft-solar-backend   - Node.js 后端 API
• fft-solar-db        - PostgreSQL 数据库

🎮 常用操作
═══════════════════════════════════════════════════════════
启动服务:     docker-start.bat
停止服务:     docker-stop.bat
查看日志:     docker-logs.bat
查看状态:     docker-compose ps

📝 Docker 命令行
═══════════════════════════════════════════════════════════
# 启动所有服务
docker-compose up -d

# 停止所有服务
docker-compose down

# 查看实时日志
docker-compose logs -f

# 重启服务
docker-compose restart

# 查看运行状态
docker-compose ps

# 进入后端容器
docker exec -it fft-solar-backend sh

# 进入数据库
docker exec -it fft-solar-db psql -U postgres -d fft_solar_crm

🔧 故障排查
═══════════════════════════════════════════════════════════
问题: 容器无法启动
解决: 1. 检查 Docker Desktop 是否运行
      2. 运行 docker-compose logs 查看错误
      3. 运行 docker-compose down -v 清理后重试

问题: 端口被占用
解决: 1. 修改 docker-compose.yml 中的端口
      2. 或关闭占用端口的程序

问题: 前端无法访问
解决: 1. 等待 30 秒让服务完全启动
      2. 查看日志 docker-compose logs frontend

💾 数据管理
═══════════════════════════════════════════════════════════
# 备份数据库
docker exec fft-solar-db pg_dump -U postgres fft_solar_crm > backup.sql

# 恢复数据库
docker exec -i fft-solar-db psql -U postgres fft_solar_crm < backup.sql

# 清理所有数据（谨慎！）
docker-compose down -v

🔄 更新系统
═══════════════════════════════════════════════════════════
1. git pull (如果使用 Git)
2. docker-compose build --no-cache
3. docker-compose up -d

📊 监控
═══════════════════════════════════════════════════════════
# 查看资源使用
docker stats

# 查看容器详情
docker inspect fft-solar-backend

# 查看网络
docker network inspect fft-solar-help_fft-solar-network

🌟 优势
═══════════════════════════════════════════════════════════
✓ 一键部署，无需手动配置环境
✓ 环境隔离，不影响系统其他应用
✓ 数据持久化，重启不丢失
✓ 易于迁移，可在任何支持 Docker 的系统运行
✓ 便于扩展，可轻松增加实例

📚 详细文档
═══════════════════════════════════════════════════════════
完整指南: DOCKER_DEPLOYMENT.md
快速开始: 快速开始.txt
项目说明: README.md

⚠️ 注意事项
═══════════════════════════════════════════════════════════
• 首次部署需要下载镜像，需要网络连接
• 确保有足够的磁盘空间（至少 10GB）
• Docker Desktop 需要至少 4GB 内存配置
• 生产环境请修改默认密码和密钥

🎯 下一步
═══════════════════════════════════════════════════════════
1. 双击 "docker-deploy.bat" 开始部署
2. 等待部署完成（约 5-10 分钟）
3. 访问 http://localhost:3201
4. 使用 admin/admin123 登录
5. 开始使用系统！

═══════════════════════════════════════════════════════════
祝使用愉快！🎉

有问题？查看 DOCKER_DEPLOYMENT.md 获取详细帮助
