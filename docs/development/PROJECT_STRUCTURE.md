# FFT Solar CRM - 项目结构说明

## 📁 完整目录结构

```
fft-solar-help/
│
├── 📄 package.json                # 根配置文件
├── 📄 .env.example               # 环境变量示例
├── 📄 .gitignore                 # Git忽略配置
│
├── 📚 文档文件/
│   ├── README.md                 # 项目说明（必读）
│   ├── START_HERE.md             # 快速启动指南 👈 从这里开始
│   ├── QUICKSTART.md             # 5分钟快速上手
│   ├── DEVELOPMENT.md            # 开发指南
│   ├── DEPLOYMENT.md             # 部署指南
│   ├── PROJECT_SUMMARY.md        # 项目总结
│   ├── COMPLETION_SUMMARY.md     # 完成总结
│   ├── FILE_LIST.md              # 文件清单
│   ├── SCRIPTS.md                # 脚本说明
│   └── PROJECT_STRUCTURE.md      # 本文件
│
├── 🗄️ database/                  # 数据库相关
│   ├── schema.sql                # 数据库表结构 + 示例数据
│   └── init-admin.js             # 密码生成工具
│
├── 🔧 server/                    # 后端代码
│   │
│   ├── config/                   # 配置
│   │   └── database.js           # 数据库连接配置
│   │
│   ├── controllers/              # 控制器（业务逻辑）
│   │   ├── authController.js     # 认证控制器
│   │   ├── clientController.js   # 甲方管理
│   │   ├── staffController.js    # 员工管理
│   │   ├── projectController.js  # 项目管理 ⭐核心
│   │   ├── vehicleController.js  # 车辆管理
│   │   ├── assetController.js    # 资产管理
│   │   ├── financeController.js  # 财务管理
│   │   └── dashboardController.js # Dashboard
│   │
│   ├── middleware/               # 中间件
│   │   └── auth.js              # JWT认证中间件
│   │
│   ├── models/                   # 数据模型
│   │   ├── index.js             # 模型聚合 + 关系定义
│   │   ├── User.js              # 用户模型
│   │   ├── Client.js            # 甲方模型
│   │   ├── Staff.js             # 员工模型
│   │   ├── Project.js           # 项目模型
│   │   ├── ProjectInverter.js   # 项目逆变器
│   │   ├── ProjectAssignment.js # 人员分配
│   │   ├── ProjectProgress.js   # 施工进度
│   │   ├── Vehicle.js           # 车辆
│   │   ├── VehicleUsage.js      # 车辆使用记录
│   │   ├── VehicleMaintenance.js # 车辆维护
│   │   ├── Asset.js             # 资产
│   │   └── FinanceRecord.js     # 财务记录
│   │
│   ├── routes/                   # 路由定义
│   │   ├── index.js             # 路由聚合
│   │   ├── auth.js              # 认证路由
│   │   ├── clients.js           # 甲方路由
│   │   ├── staff.js             # 员工路由
│   │   ├── projects.js          # 项目路由
│   │   ├── vehicles.js          # 车辆路由
│   │   ├── assets.js            # 资产路由
│   │   ├── finance.js           # 财务路由
│   │   └── dashboard.js         # Dashboard路由
│   │
│   ├── utils/                    # 工具函数
│   │   └── emailService.js      # 邮件发送服务
│   │
│   └── index.js                  # 服务器入口 ⭐启动点
│
└── 💻 client/                    # 前端代码
    │
    ├── public/
    │   └── index.html            # HTML模板
    │
    ├── src/
    │   │
    │   ├── components/           # 共用组件
    │   │   ├── Layout.js        # 主布局（侧边栏+顶栏）
    │   │   └── PrivateRoute.js  # 受保护路由
    │   │
    │   ├── context/              # React Context
    │   │   └── AuthContext.js   # 认证上下文
    │   │
    │   ├── pages/                # 页面组件
    │   │   │
    │   │   ├── Login.js         # 登录页
    │   │   ├── Dashboard.js     # Dashboard首页 ⭐
    │   │   │
    │   │   ├── Clients/         # 甲方管理
    │   │   │   ├── ClientList.js
    │   │   │   └── ClientForm.js
    │   │   │
    │   │   ├── Staff/           # 员工管理
    │   │   │   ├── StaffList.js
    │   │   │   └── StaffForm.js
    │   │   │
    │   │   ├── Projects/        # 项目管理
    │   │   │   ├── ProjectList.js
    │   │   │   ├── ProjectForm.js
    │   │   │   └── ProjectDetail.js ⭐核心页面
    │   │   │
    │   │   ├── Vehicles/        # 车辆管理
    │   │   │   └── VehicleList.js
    │   │   │
    │   │   ├── Assets/          # 资产管理
    │   │   │   └── AssetList.js
    │   │   │
    │   │   └── Finance/         # 财务管理
    │   │       └── FinanceOverview.js
    │   │
    │   ├── services/             # API服务
    │   │   └── api.js           # Axios封装
    │   │
    │   ├── App.js               # 主应用组件
    │   └── index.js             # 前端入口 ⭐启动点
    │
    └── package.json             # 前端配置文件
```

---

## 📊 文件统计

### 代码文件
```
后端:  32 个文件
前端:  19 个文件
文档:  10 个文件
配置:   5 个文件
------
总计:  66 个文件
```

### 代码行数
```
后端代码: ~4,000 行
前端代码: ~3,500 行
数据库:    ~300 行
文档:     ~2,500 行
--------
总计:   ~10,300 行
```

---

## 🎯 核心文件说明

### 必读文件（启动前）

| 文件 | 作用 | 优先级 |
|------|------|--------|
| `START_HERE.md` | 3分钟快速启动 | ⭐⭐⭐ |
| `.env.example` | 环境变量配置模板 | ⭐⭐⭐ |
| `database/schema.sql` | 数据库表结构 | ⭐⭐⭐ |

### 后端核心文件

| 文件 | 作用 | 重要性 |
|------|------|--------|
| `server/index.js` | 服务器入口 | ⭐⭐⭐ |
| `server/models/index.js` | 模型关系定义 | ⭐⭐⭐ |
| `server/controllers/projectController.js` | 项目核心逻辑 | ⭐⭐⭐ |
| `server/middleware/auth.js` | 认证中间件 | ⭐⭐ |
| `server/utils/emailService.js` | 邮件服务 | ⭐⭐ |

### 前端核心文件

| 文件 | 作用 | 重要性 |
|------|------|--------|
| `client/src/index.js` | 前端入口 | ⭐⭐⭐ |
| `client/src/App.js` | 路由配置 | ⭐⭐⭐ |
| `client/src/components/Layout.js` | 主布局 | ⭐⭐⭐ |
| `client/src/pages/Projects/ProjectDetail.js` | 项目详情（5个Tab） | ⭐⭐⭐ |
| `client/src/pages/Dashboard.js` | 首页Dashboard | ⭐⭐ |
| `client/src/services/api.js` | API封装 | ⭐⭐ |

---

## 🔄 数据流图

```
┌─────────────┐
│   Browser   │  用户界面
└──────┬──────┘
       │
       ├─> Login (JWT认证)
       │
       ├─> Dashboard (查看统计)
       │
       ├─> Projects (项目管理)
       │     └─> ProjectDetail (5个Tab)
       │
       ├─> Clients (甲方管理)
       │
       ├─> Staff (员工管理)
       │
       ├─> Finance (财务管理)
       │
       └─> Vehicles/Assets

           ↓ HTTP请求

┌─────────────┐
│  React App  │  前端应用
│             │
│  api.js ────┼─> Axios封装
└──────┬──────┘
       │
       │ HTTP API调用
       │
┌──────▼──────┐
│   Express   │  后端服务器
│   Server    │
│             │
│  Routes ────┼─> 路由分发
│  Controllers├─> 业务逻辑
│  Models ────┼─> 数据模型
└──────┬──────┘
       │
       │ SQL查询
       │
┌──────▼──────┐
│ PostgreSQL  │  数据库
│   Database  │
│             │
│  12张表    │
│  示例数据  │
└─────────────┘
```

---

## 🗃️ 数据库表关系

```
users (管理员)

clients (甲方) ──┐
                 │
                 ├─> projects (项目) ──┐
                 │                      │
staff (员工) ───┼─────────────────────┼─> project_assignments (人员分配)
                 │                      │
                 │                      ├─> project_inverters (逆变器)
                 │                      │
                 │                      ├─> project_progress (施工进度)
                 │                      │
                 │                      └─> finance_records (财务记录)
                 │
vehicles (车辆) ─┼─> vehicle_usage (使用记录)
                 │
                 └─> vehicle_maintenance (维护记录)

assets (资产)

finance_records (财务记录)
```

---

## 📋 API 端点结构

```
/api
├── /auth
│   ├── POST   /login         # 登录
│   ├── GET    /me            # 获取当前用户
│   └── POST   /logout        # 登出
│
├── /clients
│   ├── GET    /              # 获取列表
│   ├── POST   /              # 创建
│   ├── GET    /:id           # 获取详情
│   ├── PUT    /:id           # 更新
│   ├── DELETE /:id           # 删除
│   └── GET    /:id/projects  # 获取项目
│
├── /staff
│   ├── GET    /              # 获取列表
│   ├── POST   /              # 创建
│   ├── GET    /:id           # 获取详情
│   ├── PUT    /:id           # 更新
│   ├── DELETE /:id           # 删除
│   └── GET    /:id/payments  # 薪资历史
│
├── /projects
│   ├── GET    /                          # 获取列表
│   ├── POST   /                          # 创建
│   ├── GET    /:id                       # 获取详情
│   ├── PUT    /:id                       # 更新
│   ├── DELETE /:id                       # 删除
│   ├── POST   /:id/inverters             # 添加逆变器
│   ├── POST   /:id/assignments           # 分配人员
│   ├── POST   /:id/notify                # 发送通知
│   ├── GET    /:id/progress              # 获取进度
│   ├── PUT    /:id/progress/:stage       # 更新进度
│   └── GET    /:id/finance               # 财务明细
│
├── /vehicles
│   ├── GET    /                          # 获取列表
│   ├── POST   /                          # 创建
│   ├── GET    /:id                       # 获取详情
│   ├── POST   /:id/usage                 # 使用记录
│   └── POST   /:id/maintenance           # 维护记录
│
├── /assets
│   ├── GET    /              # 获取列表
│   ├── POST   /              # 创建
│   ├── GET    /:id           # 获取详情
│   └── PUT    /:id           # 更新
│
├── /finance
│   ├── GET    /records       # 财务记录
│   ├── POST   /records       # 创建记录
│   ├── GET    /summary       # 财务汇总 ⭐
│   ├── GET    /projects      # 项目报表
│   └── GET    /staff         # 员工报表
│
└── /dashboard
    ├── GET    /overview      # 概览数据 ⭐
    └── GET    /charts        # 图表数据
```

---

## 🎨 前端路由结构

```
/ (Layout)
├── /                         # Dashboard
├── /login                    # 登录页（公开）
│
├── /clients                  # 甲方列表
├── /clients/new              # 新建甲方
└── /clients/:id/edit         # 编辑甲方
│
├── /staff                    # 员工列表
├── /staff/new                # 新建员工
└── /staff/:id/edit           # 编辑员工
│
├── /projects                 # 项目列表
├── /projects/new             # 新建项目
├── /projects/:id             # 项目详情 ⭐
└── /projects/:id/edit        # 编辑项目
│
├── /vehicles                 # 车辆列表
├── /assets                   # 资产列表
└── /finance                  # 财务管理
```

---

## 🔑 关键技术点

### 后端技术

1. **Sequelize ORM**
   - 自动 timestamps
   - VIRTUAL 字段计算
   - 模型关系定义

2. **JWT 认证**
   - Token 生成
   - 中间件验证
   - 自动续期

3. **自动计算**
   - 项目收入
   - 员工薪资
   - 项目利润

### 前端技术

1. **Material-UI**
   - 现代化组件
   - 响应式设计
   - 主题定制

2. **React Router**
   - 嵌套路由
   - 受保护路由
   - 编程导航

3. **Axios**
   - 请求拦截
   - 响应拦截
   - 自动添加Token

---

## 📦 依赖包说明

### 后端核心依赖
```json
{
  "express": "Web框架",
  "pg": "PostgreSQL驱动",
  "sequelize": "ORM框架",
  "bcryptjs": "密码加密",
  "jsonwebtoken": "JWT认证",
  "nodemailer": "邮件服务"
}
```

### 前端核心依赖
```json
{
  "react": "UI框架",
  "@mui/material": "UI组件库",
  "react-router-dom": "路由管理",
  "axios": "HTTP客户端"
}
```

---

## 🎯 开发建议

### 添加新功能

1. **后端**:
   - 在 `models/` 添加模型
   - 在 `controllers/` 添加控制器
   - 在 `routes/` 添加路由
   - 在 `routes/index.js` 注册

2. **前端**:
   - 在 `pages/` 添加页面
   - 在 `App.js` 添加路由
   - 在 `Layout.js` 添加菜单

### 修改样式

- 修改 `index.js` 的 theme 配置
- 使用 MUI 的 sx 属性

### 调试技巧

- 后端: `console.log` + Postman
- 前端: Chrome DevTools + React DevTools
- 数据库: pgAdmin

---

**项目结构清晰，易于理解和维护！** 🎉

---

*最后更新: 2025年1月5日*
